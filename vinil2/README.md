I built a complete inventory management system for vinyl records.

A web app that manages inventory, records sales, automatically calculates profit, and even retrieves record data directly from the Discogs API.

Pure JavaScript, Firebase Firestore for cloud persistence and integration with an external API.
